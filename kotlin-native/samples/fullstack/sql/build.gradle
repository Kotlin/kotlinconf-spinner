apply plugin: 'konan'

konanArtifacts {
    interop('sqlite3') {
        defFile 'src/sqlite3.def'
    }

    library('ksqlite') {
        srcDir 'src'

        libraries {
            artifact 'sqlite3'
        }
    }
}